from mimetypes import init


请读取 memory-bank 中所有文件，尤其是 implementation-plan.md、progress.md 和 architecture.md。
这次只执行 implementation-plan.md 中的 Step 2.1，注意：
  • 只能改与本 Step 直接相关的文件；
  • 每个文件改动尽量聚焦，不要顺手重构太多东西。

执行流程：
1. 简要复述 Step 2.1 的目标和你打算改哪些文件；
2. 给出具体代码修改（带文件路径）；
3. 告诉我如何本地自测（给出完整 CLI 命令）。

请读取 memory-bank 中所有文件，尤其是 implementation-plan.md、progress.md 和 architecture.md。
这次只执行 implementation-plan.md 中的 Step 3a，注意：
  • 只能改与本 Step 直接相关的文件；
  • 每个文件改动尽量聚焦，不要顺手重构太多东西。

执行流程：
1. 简要复述 Step 3a 的目标和你打算改哪些文件；
2. 给出具体代码修改（带文件路径）；
3. 告诉我如何本地自测

暂时不要更新 progress.md 和 architecture.md，等我本地测通过后再一起改。



我已经按你给的命令在本地测试过，Step 3a 功能正常。 但是质量还是略差。也许需要引入人工标注来提升质量。以及增加其他的如 bm25关键词限制或者 hybrid 等策略。不过这些都是后话，
请现在：
1. 更新 memory-bank/progress.md，增加一条记录；
2. 更新 memory-bank/architecture.md，把本次新增/修改的模块和文件补充进去。

输出时请给出这两个文件的完整新内容，方便我复制粘贴覆盖。

请先读取项目根目录下的 memory-bank/prd.md、memory-bank/tech-stack.md、memory-bank/architecture.md 和 memory-bank/implementation-plan.md。
确认你理解 QAnchor Phase/Stage 1 的目标后，检查 implementation-plan.md 的 Step 列表是否合理，有任何疑问先问我再改。
当前不要写代码，只输出：
1. 你对项目目标和范围的理解（不超过 10 行）；
2. 你认为 所有 Step 中最可能踩坑的 2–3 个地方；
3. 如果要为我这样的个人开发者优化 Step 粒度，你会怎么改（可以重命名或拆/并 Steps）。 

请先读取项目根目录下的 memory-bank/prd.md、memory-bank/tech-stack.md、memory-bank/architecture.md 和 memory-bank/implementation-plan.md。
确认你理解 QAnchor  的目标后，检查 implementation-plan.md 的 Step 列表是否合理，有任何疑问先问我再改。
当前不要写代码，只输出：
1. 你对项目目标和范围的理解（不超过 10 行）；
2. 你认为 所有 Step 中最可能踩坑的 2–3 个地方；
3. 如果要为我这样的个人开发者优化 Step 粒度，你会怎么改（可以重命名或拆/并 Steps）。

